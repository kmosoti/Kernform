# Project signatures

Each `signature.toml` declares one composable project form. Kernform resolves
implication and runtime selection in the Rust core before capability rendering.
Signature manifests are data only and cannot execute code.
