{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:kernform:schema:state:v2",
  "title": "Kernform managed state v2",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "generator_version", "project_root", "requested_signatures", "resolved_signatures", "default_signature", "manifest_hash", "toolchains", "files"],
  "properties": {
    "schema": {"const": "kernform.state/v2"},
    "generator_version": {"type": "string", "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$"},
    "project_root": {"type": "string", "minLength": 1},
    "requested_signatures": {"type": "array", "items": {"$ref": "#/$defs/signature"}, "minItems": 1, "uniqueItems": true},
    "resolved_signatures": {"type": "array", "items": {"$ref": "#/$defs/signature"}, "minItems": 1, "uniqueItems": true},
    "default_signature": {"anyOf": [{"$ref": "#/$defs/signature"}, {"type": "null"}]},
    "manifest_hash": {"$ref": "#/$defs/sha256"},
    "toolchains": {
      "type": "object",
      "additionalProperties": false,
      "required": ["catalog_id", "catalog_hash"],
      "properties": {
        "catalog_id": {"type": "string", "minLength": 1},
        "catalog_hash": {"$ref": "#/$defs/sha256"}
      }
    },
    "files": {
      "type": "array",
      "items": {"$ref": "#/$defs/file"}
    }
  },
  "$defs": {
    "signature": {"enum": ["sdk", "cli", "api", "interactive-web", "daemon"]},
    "sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "file": {
      "type": "object",
      "additionalProperties": false,
      "required": ["path", "hash", "ownership"],
      "properties": {
        "path": {"type": "string", "minLength": 1, "not": {"pattern": "(^|/)\\.git(/|$)"}},
        "hash": {"$ref": "#/$defs/sha256"},
        "ownership": {"enum": ["managed", "seeded", "generated", "user", "external"]}
      }
    }
  }
}
