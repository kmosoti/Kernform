# example-interactive-web

Generated by Kernform 0.2.0 with signatures ["interactive-web"].

```bash
uv sync --all-groups
uv run pytest
```

Local Git starts on `main` without a commit or remote unless explicitly requested.
