# Naming

Use `Kernform` for the product, `kernform` for the Python distribution, package, CLI, configuration
root, and OCI image prefix, and `kernform-*` for Rust crates. Historical names belong only in the
history page.
