# {{ project_name }}

Generated by Kernform 0.1.0 with the `{{ profile }}` profile.

```bash
uv sync --all-groups
uv run pytest
```

Local Git starts on `main` without a commit or remote unless explicitly requested.
