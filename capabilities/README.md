# Capabilities

Each capability owns one `capability.toml` manifest and deterministic static resources. Manifests
declare dependencies, conflicts, files, structured patches, tests, and conformance rules. They do
not contain executable scripts.
